const Reports=(()=>{function initialize(){console.log("Reports module ready.")}function latestTrip(){const trips=Storage.trips();const current=Storage.currentTrip();if(current)return current;if(!trips.length)return null;return trips[trips.length-1]}function tripSummary(trip){if(!trip)return null;const catches=Storage.catches().filter(f=>f.tripId===trip.id);const total=catches.length;const biggest=catches.length?catches.reduce((a,b)=>(Number(b.weight)||0)>(Number(a.weight)||0)?b:a):null;const avg=total?catches.reduce((s,f)=>s+(Number(f.weight)||0),0)/total:0;const topBait=top(catches,"bait");const topCover=top(catches,"cover");const topColor=top(catches,"color");const duration=trip.endTime?Math.max(0,Math.round((new Date(trip.endTime)-new Date(trip.startTime))/60000)):Math.max(0,Math.round((Date.now()-new Date(trip.startTime).getTime())/60000));const takeaway=total?`${topBait?topBait.name:"Top bait"} around ${topCover?topCover.name:"cover"} was the main pattern.`:"No fish logged on this trip yet.";return{trip,catches,total,biggest,avg,topBait,topCover,topColor,duration,takeaway,shareText:`2° Logbook Trip Report\n\nSpot: ${trip.lake}\nFish: ${total}\nBiggest: ${biggest?Number(biggest.weight).toFixed(2)+" lb":"None"}\nAverage: ${avg.toFixed(2)} lb\nBest bait: ${topBait?topBait.name:"None"}\nBest color: ${topColor?topColor.name:"None"}\nBest cover: ${topCover?topCover.name:"None"}\n\nTakeaway: ${takeaway}`}}function top(catches,key){const map={};catches.forEach(f=>{const name=f[key]||"Unknown";map[name]=(map[name]||0)+1});const sorted=Object.entries(map).sort((a,b)=>b[1]-a[1]);return sorted.length?{name:sorted[0][0],count:sorted[0][1]}:null}return{initialize,latestTrip,tripSummary}})();