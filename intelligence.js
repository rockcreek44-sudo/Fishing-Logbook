const Intelligence=(()=>{function initialize(){console.log("Intelligence module ready.")}function report(){const fish=Storage.catches();if(!fish.length)return empty();const baitSignals=signals(fish,"bait");const colorSignals=signals(fish,"color");const trailerSignals=signals(fish,"trailer");const coverSignals=signals(fish,"cover");const comboSignals=comboSignalsFn(fish);const streaks=hotCold(fish);const big=bigFish(fish);const coach=plainEnglish(fish,baitSignals,colorSignals,trailerSignals,coverSignals,comboSignals,streaks,big);return{fishCount:fish.length,baitSignals,colorSignals,trailerSignals,coverSignals,comboSignals,streaks,big,coach}}function empty(){return{fishCount:0,baitSignals:[],colorSignals:[],trailerSignals:[],coverSignals:[],comboSignals:[],streaks:{hot:[],cold:[]},big:{quality:0,trophy:0,bestBait:null,bestCombo:null},coach:["Log catches to unlock the fishing intelligence engine."]}}function signals(fish,key){const map={};fish.forEach(f=>{const name=f[key]||"Unknown";if(!map[name])map[name]={name,count:0,totalWeight:0,biggest:0,quality:0,trophy:0,recent:0};map[name].count++;map[name].totalWeight+=Number(f.weight)||0;map[name].biggest=Math.max(map[name].biggest,Number(f.weight)||0);if((Number(f.weight)||0)>=3)map[name].quality++;if((Number(f.weight)||0)>=5)map[name].trophy++});const recent=fish.slice(-10);recent.forEach(f=>{const name=f[key]||"Unknown";if(map[name])map[name].recent++});return Object.values(map).map(x=>{const avg=x.count?x.totalWeight/x.count:0;const score=Math.round((x.count*10)+(avg*12)+(x.biggest*10)+(x.quality*8)+(x.trophy*15)+(x.recent*5));return{...x,avg,score,confidence:Math.min(99,Math.max(25,score))}}).sort((a,b)=>b.score-a.score)}function comboSignalsFn(fish){const map={};fish.forEach(f=>{const name=[f.bait||"Unknown",f.color||"Unknown",f.trailer||"Unknown",f.cover||"Unknown"].join(" • ");if(!map[name])map[name]={name,count:0,totalWeight:0,biggest:0,quality:0,trophy:0,recent:0};map[name].count++;map[name].totalWeight+=Number(f.weight)||0;map[name].biggest=Math.max(map[name].biggest,Number(f.weight)||0);if((Number(f.weight)||0)>=3)map[name].quality++;if((Number(f.weight)||0)>=5)map[name].trophy++});fish.slice(-10).forEach(f=>{const name=[f.bait||"Unknown",f.color||"Unknown",f.trailer||"Unknown",f.cover||"Unknown"].join(" • ");if(map[name])map[name].recent++});return Object.values(map).map(x=>{const avg=x.count?x.totalWeight/x.count:0;const score=Math.round((x.count*12)+(avg*14)+(x.biggest*10)+(x.quality*10)+(x.trophy*18)+(x.recent*6));return{...x,avg,score,confidence:Math.min(99,Math.max(25,score))}}).sort((a,b)=>b.score-a.score)}function hotCold(fish){const recent=fish.slice(-10);const older=fish.slice(0,Math.max(0,fish.length-10));const recentBaits=counts(recent,"bait");const olderBaits=counts(older,"bait");const names=[...new Set([...Object.keys(recentBaits),...Object.keys(olderBaits)])];const compared=names.map(name=>({name,recent:recentBaits[name]||0,older:olderBaits[name]||0,change:(recentBaits[name]||0)-(olderBaits[name]||0)}));return{hot:compared.filter(x=>x.recent>0).sort((a,b)=>b.recent-a.recent).slice(0,3),cold:compared.filter(x=>x.older>0&&x.recent===0).sort((a,b)=>b.older-a.older).slice(0,3)}}function counts(fish,key){const map={};fish.forEach(f=>{const n=f[key]||"Unknown";map[n]=(map[n]||0)+1});return map}function bigFish(fish){const quality=fish.filter(f=>(Number(f.weight)||0)>=3);const trophy=fish.filter(f=>(Number(f.weight)||0)>=5);return{quality:quality.length,trophy:trophy.length,bestBait:signals(quality,"bait")[0]||null,bestCombo:comboSignalsFn(quality)[0]||null,biggest:fish.slice().sort((a,b)=>(Number(b.weight)||0)-(Number(a.weight)||0))[0]||null}}function plainEnglish(fish,baits,colors,trailers,covers,combos,streaks,big){const out=[];const bestCombo=combos[0];const bestBait=baits[0];if(bestCombo)out.push(`Your strongest full pattern is ${bestCombo.name}. It has ${bestCombo.count} fish, averages ${bestCombo.avg.toFixed(2)} lb, and has a ${bestCombo.confidence}% confidence score.`);if(bestBait)out.push(`${bestBait.name} is your best overall bait signal right now.`);if(big.bestBait)out.push(`For better fish, ${big.bestBait.name} has the strongest 3+ pound signal.`);if(streaks.hot.length)out.push(`Hot lately: ${streaks.hot.map(x=>x.name).join(", ")}.`);if(streaks.cold.length)out.push(`Cold lately: ${streaks.cold.map(x=>x.name).join(", ")}.`);if(fish.length<10)out.push("The data is still young. Log at least 10 catches before treating these scores as strong.");return out}return{initialize,report}})();