const Baits=(()=>{function initialize(){console.log("Baits module ready.")}function makeId(name){return"bait_"+String(name||"bait").toLowerCase().replace(/[^a-z0-9]+/g,"_").replace(/^_+|_+$/g,"")+"_"+Date.now()}function list(){return Storage.baits()}function add(data){const bait={id:makeId(data.name),name:data.name||"Unnamed Bait",colors:splitList(data.colors),trailers:splitList(data.trailers),weights:splitList(data.weights),notes:data.notes||""};Storage.addBait(bait);return bait}function remove(id){Storage.removeBait(id)}function findByName(name){return list().find(bait=>bait.name===name)}function splitList(value){return String(value||"").split(",").map(item=>item.trim()).filter(Boolean)}function optionsFor(name){const bait=findByName(name);if(!bait)return{colors:[],trailers:[],weights:[]};return{colors:bait.colors||[],trailers:bait.trailers||[],weights:bait.weights||[]}}return{initialize,list,add,remove,findByName,optionsFor}})();