const UI=(()=>{const pages={};function initialize(){cachePages();bindNavigation();bindForms();populateBaitSelect();render();prefillCatchForm();show("home");console.log("UI module ready.")}function cachePages(){document.querySelectorAll("[data-page]").forEach(page=>pages[page.id]=page)}function bindNavigation(){document.querySelectorAll("[data-nav]").forEach(button=>button.addEventListener("click",()=>show(button.dataset.nav)))}function bindForms(){const startTripForm=document.getElementById("startTripForm");const catchForm=document.getElementById("catchForm");const endTripButton=document.getElementById("endTripButton");const baitForm=document.getElementById("baitForm");const catchBait=document.getElementById("catchBait");if(startTripForm){startTripForm.addEventListener("submit",event=>{event.preventDefault();Trips.create({lake:getValue("tripLake"),weather:getValue("tripWeather"),waterClarity:getValue("tripWaterClarity"),notes:getValue("tripNotes")});render();prefillCatchForm();show("catch")})}if(catchBait)catchBait.addEventListener("change",()=>populateBaitDetails(catchBait.value));if(catchForm){catchForm.addEventListener("submit",event=>{event.preventDefault();try{Catches.create({bait:getValue("catchBait"),color:getValue("catchColor"),trailer:getValue("catchTrailer"),weight:getValue("catchWeight"),length:getValue("catchLength"),cover:getValue("catchCover"),depth:getValue("catchDepth"),notes:getValue("catchNotes")});clearCatchMeasurementsOnly();prefillCatchForm();render();show("home")}catch{alert("Start a trip first.");show("trip")}})}if(endTripButton)endTripButton.addEventListener("click",()=>{Trips.finish();render();show("home")});if(baitForm){baitForm.addEventListener("submit",event=>{event.preventDefault();Baits.add({name:getValue("baitName"),colors:getValue("baitColors"),trailers:getValue("baitTrailers"),weights:getValue("baitWeights"),notes:getValue("baitNotes")});baitForm.reset();populateBaitSelect();renderBaits()})}}function show(pageId){Object.values(pages).forEach(page=>page.classList.remove("active"));if(pages[pageId])pages[pageId].classList.add("active");document.querySelectorAll("[data-nav]").forEach(button=>button.classList.toggle("active",button.dataset.nav===pageId));if(pageId==="catch")prefillCatchForm();if(pageId==="baits")renderBaits()}function render(){renderDashboard();renderTripStatus();renderHistory();renderStats();renderBaits()}function renderDashboard(){const summary=Stats.summary();const biggest=summary.biggestFish;setText("totalFish",summary.totalFish);setText("averageWeight",summary.averageWeight.toFixed(2));setText("confidenceBait",summary.confidenceBait||"No data");setText("biggestFish",biggest?`${biggest.weight.toFixed(2)} lb`:"None")}function renderTripStatus(){const trip=Trips.current();setText("currentTripStatus",trip?`${trip.lake} • ${Trips.elapsedMinutes()} min`:"No active trip")}function renderHistory(){const container=document.getElementById("tripHistory");if(!container)return;const trips=Storage.trips();if(!trips.length){container.innerHTML=`<div class="card empty">No trips logged yet.</div>`;return}container.innerHTML=trips.slice().reverse().map(trip=>{const catches=Catches.trip(trip.id);return`<div class="card"><h3>${escapeHTML(trip.lake)}</h3><p class="small-text">${new Date(trip.startTime).toLocaleString()}</p><p>${catches.length} fish</p><p>${escapeHTML(trip.waterClarity)} • ${escapeHTML(trip.weather)}</p></div>`}).join("")}function renderStats(){const container=document.getElementById("baitStats");if(!container)return;const performance=Stats.baitPerformance();const names=Object.keys(performance);if(!names.length){container.innerHTML=`<div class="card empty">No bait data yet.</div>`;return}container.innerHTML=names.map(name=>{const item=performance[name];return`<div class="card"><h3>${escapeHTML(name)}</h3><p>${item.fish} fish</p><p>Average: ${item.averageWeight.toFixed(2)} lb</p></div>`}).join("")}function renderBaits(){const container=document.getElementById("baitManagerList");if(!container)return;const baits=Baits.list();if(!baits.length){container.innerHTML=`<div class="card empty">No baits saved yet.</div>`;return}container.innerHTML=baits.map(bait=>`<div class="card"><h3>${escapeHTML(bait.name)}</h3><p>${escapeHTML(bait.notes||"")}</p><div class="pills">${(bait.colors||[]).map(c=>`<span class="pill">${escapeHTML(c)}</span>`).join("")}</div><div class="pills">${(bait.trailers||[]).map(t=>`<span class="pill">${escapeHTML(t)}</span>`).join("")}</div><div class="pills">${(bait.weights||[]).map(w=>`<span class="pill">${escapeHTML(w)}</span>`).join("")}</div><button class="small-button" data-delete-bait="${escapeHTML(bait.id)}">Delete</button></div>`).join("");container.querySelectorAll("[data-delete-bait]").forEach(button=>{button.addEventListener("click",()=>{Baits.remove(button.dataset.deleteBait);populateBaitSelect();renderBaits()})})}function populateBaitSelect(){const baitSelect=document.getElementById("catchBait");if(!baitSelect)return;const setup=Storage.lastSetup();const current=baitSelect.value||setup.bait;baitSelect.innerHTML=Baits.list().map(bait=>`<option>${escapeHTML(bait.name)}</option>`).join("");setValue("catchBait",current);if(!baitSelect.value&&Baits.list()[0])setValue("catchBait",Baits.list()[0].name);populateBaitDetails(baitSelect.value)}function populateBaitDetails(baitName){const setup=Storage.lastSetup();const options=Baits.optionsFor(baitName);fillSelect("catchColor",options.colors,setup.color);fillSelect("catchTrailer",options.trailers,setup.trailer)}function fillSelect(id,options,preferred){const select=document.getElementById(id);if(!select)return;const values=options&&options.length?options:[preferred||""];select.innerHTML=values.map(value=>`<option>${escapeHTML(value)}</option>`).join("");if(preferred&&values.includes(preferred))setValue(id,preferred)}function prefillCatchForm(){const setup=Storage.lastSetup?Storage.lastSetup():null;if(!setup)return;populateBaitSelect();setValue("catchBait",setup.bait||"Workhorse");populateBaitDetails(setup.bait||"Workhorse");setValue("catchColor",setup.color||"");setValue("catchTrailer",setup.trailer||"");setValue("catchCover",setup.cover||"Grass");setValue("catchDepth",setup.depth||"")}function clearCatchMeasurementsOnly(){setValue("catchWeight","");setValue("catchLength","");setValue("catchNotes","")}function getValue(id){const el=document.getElementById(id);return el?el.value.trim():""}function setValue(id,value){const el=document.getElementById(id);if(el)el.value=value}function setText(id,value){const el=document.getElementById(id);if(el)el.textContent=value}function escapeHTML(value){return String(value||"").replace(/[&<>\"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]))}return{initialize,render,show}})();