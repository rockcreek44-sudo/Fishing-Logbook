const Trips=(()=>{let activeTrip=null;function makeId(){return crypto&&crypto.randomUUID?crypto.randomUUID():"id_"+Date.now()+"_"+Math.random().toString(16).slice(2)}function initialize(){activeTrip=Storage.currentTrip()}function create(data){if(activeTrip)return activeTrip;activeTrip={id:makeId(),lake:data.lake||"Unnamed Spot",startTime:new Date().toISOString(),endTime:null,weather:data.weather||"",waterClarity:data.waterClarity||"",waterTemp:Number(data.waterTemp)||0,wind:data.wind||"",notes:data.notes||"",catches:[],photos:[],completed:false};Storage.setCurrentTrip(activeTrip);return activeTrip}function current(){return activeTrip}function addCatch(catchId){if(!activeTrip)return;activeTrip.catches.push(catchId);Storage.setCurrentTrip(activeTrip)}function elapsedMinutes(){if(!activeTrip)return 0;return Math.floor((Date.now()-new Date(activeTrip.startTime).getTime())/60000)}function finish(){if(!activeTrip)return;activeTrip.endTime=new Date().toISOString();activeTrip.completed=true;Storage.setCurrentTrip(activeTrip);Storage.endTrip();activeTrip=null}return{initialize,create,current,addCatch,elapsedMinutes,finish}})();